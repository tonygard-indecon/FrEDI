name: render_markdown

on:
  push:
    branches:
      - test_dev_tkg

jobs:
  render:
    runs-on: ubuntu-latest  
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    steps:
    # checkout source code
    - uses: actions/checkout@v3
    - uses: r-lib/actions/setup-r@v3
    - uses: r-lib/actions/setup-pandoc@v3
    - uses: r-lib/actions/setup-tinytex@v3
    # install rmarkdown
    - name: Install rmarkdown
      run: |
        Rscript -e 'install.packages("rmarkdown")'
    # copy files
    - name: Render Markdown
      run: |
        Rscript -e 'rmarkdown::render("./docs/my-document.Rmd")'
    # upload new documentation
    - name: publish to github
      run: |
        git config --local core.autocrlf false
        git config --local user.email "${{ github.actor }}@users.noreply.github.com"
        git config --local user.name "${{ github.actor }}"
        git add . && git add --renormalize .
        git pull origin ${{ github.head_ref }} --autostash --rebase -X ours
        git commit --allow-empty -am "AUTO-GH ACTION: ${{ github.workflow }}"
        NO_PAGER=1 git --no-pager diff HEAD^
    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        branch: ${{ github.head_ref }}
        github_token: ${{ secrets.GITHUB_TOKEN }}